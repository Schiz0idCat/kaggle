Los archivos CSV van dentro de la carpeta data, que es ignorada por git.
Para que esto funcione en local, se debe crear la carpeta y subir los archivos manualmente.
Además, `MTI_sample.csv` fue creada a partir de eliminar aleatoreamente el 50% de filas de `MTI_train.csv`